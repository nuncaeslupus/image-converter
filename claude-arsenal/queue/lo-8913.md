# Payload: lo-8913 — T14: Wire settingsStore into TraceStep

## Acceptance gate
`settingsStore.ts` (built + unit-tested in T10/#19) is actually used: on mount
TraceStep initializes the tweak values from `load()` (falling back to defaults),
and every tweak change persists via `save()`. Verify by a test or manual check
that reloading the page restores the last-used tweak values.

## What the spec requires
Spec §5 Local storage contract + plan "State changes": last-used tweak values
persist to `localStorage` key `image-converter:last-settings:v1` and restore on
next visit.

## What the code does
`src/lib/settingsStore.ts` exists with `save`/`load` + defaults fallback, fully
unit-tested, but is imported nowhere — nothing calls save/load. TraceStep still
initializes from the hardcoded `DEFAULT_VALUES`.

## Fix location
`src/steps/TraceStep.tsx`: seed the initial `values` state from `load()` instead
of `DEFAULT_VALUES`, and call `save(next)` in `handleChange`. Keep it a small,
localized change.

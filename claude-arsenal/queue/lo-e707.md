# Payload: lo-e707 — T6: Live tweak panel driving two-tier retrace/cheap-edit pipeline

**Gate**: `tweak_pipeline_routing_accuracy == 1.0`

## Tests

- `test_tweakPipeline_paletteChange_triggersWorkerRetrace` in `tests/lib/tweakPipeline.test.ts`
- `test_tweakPipeline_backgroundChange_skipsWorkerCall` in `tests/lib/tweakPipeline.test.ts`
- `test_tweakPipeline_rapidChanges_debouncedToLastValue` in `tests/lib/tweakPipeline.test.ts`

Retrace params always call the worker, non-retrace params never do, rapid changes collapse to one call.

## References

- Spec: `status/specification.md` Goals — palette (with quick presets 1/2/3/4/8/16… + auto), smoothness, detail, contrast, background; §5 Worker message contract for exactly which params belong in a `trace` request vs. never sent to the worker.
- Plan: `status/plan.md` "Data flow" steps 3-6 — the retrace-vs-cheap-edit boundary is authoritative there; do not add new params without updating that section.
- Depends on: `lo-d4f4` (T3, real tracer) and `lo-4058` (T5, edited bitmap as tracer input).
- Sibling: `src/steps/TraceStep.tsx` — replace this T1 placeholder.

## Context

Location: `src/components/TweakPanel/*`, `src/lib/tweakPipeline.ts`. Size L — this is the biggest single UI+logic task in the plan and the fan-out point for T7/T8/T9/T10. Worth a `frontend-design` pass for the panel layout before implementation.

## Failure notes

# Payload: lo-4058 — T5: Basic image editing (crop/resize/rotate)

**Gate**: `edit_roundtrip_pixel_diff_max == 0`

## Tests

- `test_cropImage_boundingBox_matchesExpectedPixels` in `tests/lib/imageEdit.test.ts`
- `test_rotateImage_90Degrees_swapsDimensions` in `tests/lib/imageEdit.test.ts`
- `test_resizeImage_targetDimensions_matchesOutputDimensions` in `tests/lib/imageEdit.test.ts`

Each op on a fixed synthetic fixture must match its deterministically-expected output.

## References

- Spec: `status/specification.md` Goals — "standard tool placement, standard icons, standard keyboard shortcuts — not buried in a menu" for crop/resize/rotate.
- Plan: `status/plan.md` "UI flow (step wizard)" step 2 — this step is optional/skippable, defaults straight through to Trace & Tweak.
- Depends on: `lo-38a1` (T4) — operates on the `ImageBitmap` that T4's decode step produces.
- Sibling: `src/steps/EditStep.tsx` — replace this T1 placeholder; it currently just has Back/Next buttons wired to the wizard.

## Context

Location: `src/components/Editor/*`, `src/lib/imageEdit.ts`. Keyboard shortcuts and icon choices should follow standard conventions (see spec's explicit callout) — this is a UI-design-sensitive task, worth a pass with the `frontend-design` skill's guidance before implementation.

## Failure notes

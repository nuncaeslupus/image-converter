# Payload: lo-22c0 — T8: Trace-result caching by (imageId, params)

**Gate**: `cache_hit_rate_on_repeated_params == 1.0`

## Tests

- `test_traceCache_repeatedParams_returnsCachedResultWithoutWorkerCall` in `tests/lib/traceCache.test.ts`
- `test_traceCache_changedParams_missesCache` in `tests/lib/traceCache.test.ts`

Identical params within a session skip the worker; any changed param misses.

## References

- Spec: `status/specification.md §5` Worker message contract — `requestId` last-wins discard is a separate concern from caching; don't conflate them.
- Plan: `status/plan.md` "Data flow" step 5 — cache keyed by `hash(imageId, params)`; §6 risk row on cache/debounce staleness bugs.
- Depends on: `lo-e707` (T6) — sits between the tweak pipeline and the worker call.

## Context

Location: `src/lib/traceCache.ts`. Small, self-contained (Size S) — a good candidate to parallelize alongside T7/T10 once T6 lands.

## Failure notes

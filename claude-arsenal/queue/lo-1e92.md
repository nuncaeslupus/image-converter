# Payload: lo-1e92 — T9: Export (download/copy/viewBox override/size estimate)

**Gate**: `export_output_valid_svg_rate == 1.0`

## Tests

- `test_exportSvg_download_producesValidSvgBlob` in `tests/lib/svgExport.test.ts`
- `test_exportSvg_copyToClipboard_writesFullMarkup` in `tests/lib/svgExport.test.ts`
- `test_exportSvg_viewBoxOverride_appliesWithoutRetrace` in `tests/lib/svgExport.test.ts`

Exported output always parses as valid SVG; clipboard write matches rendered markup; a viewBox override never triggers a worker call.

## References

- Spec: `status/specification.md` Goals — download `.svg`, copy raw markup, override output size/viewBox, size/path-count estimate before exporting.
- Plan: `status/plan.md` "Data flow" step 8; §5 confirms viewBox/size override is a cheap SVG-only edit, never a retrace.
- Depends on: `lo-e707` (T6, current SVG) and `lo-22c0` (T8, cache — export should read the cached result, not force a retrace).
- Sibling: `src/steps/ExportStep.tsx` — replace this T1 placeholder.

## Context

Location: `src/components/Export/*`, `src/lib/svgExport.ts`.

## Failure notes

# Payload: lo-38a1 — T4: Upload & input handling (file picker/drag-drop, client-side decode)

**Gate**: `supported_format_decode_success_rate == 1.0`

## Tests

- `test_decodeImage_supportedFormats_returnsImageBitmap` in `tests/lib/imageDecode.test.ts` — each of PNG/JPEG/WebP/GIF/BMP fixtures decodes to an `ImageBitmap`.
- `test_decodeImage_unsupportedFile_rejectsWithError` in `tests/lib/imageDecode.test.ts` — a non-image file rejects with a typed error.

## References

- Spec: `status/specification.md` Goals — "PNG, JPEG, WebP, GIF, or BMP", decoded entirely client-side.
- Plan: `status/plan.md` "UI flow (step wizard)" step 1, and T4 row.
- Sibling: `src/steps/UploadStep.tsx` — replace this T1 placeholder with the real file picker / drag-and-drop UI; it currently just calls `wizard.next()`.
- Sample fixtures: add small test images per format under `tests/fixtures/` (create if absent) — needed by both this task and T3's sample corpus.

## Context

Location: `src/components/Upload.tsx` (or extend `src/steps/UploadStep.tsx` directly), `src/lib/imageDecode.ts`. This is the wizard's step 1 — no dependency on the tracer, so it can run in parallel with T2.

## Failure notes
